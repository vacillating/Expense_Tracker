# Telegram bot 专用依赖——刻意跟 requirements.txt（Streamlit Cloud 用）分开。
# 目标是跑在 serverless 函数里，bundle 体积敏感，不能把 streamlit+pandas 也
# 打进去。parser.py / bot_handlers.py / sheets.py 加起来真正需要的东西都在
# 这里，不多不少。
#
# 故意不钉版本号。requirements.txt 那份是对着 Streamlit Cloud 实际跑的
# Python（3.13.15，见 CLAUDE.md）用 `pip download --only-binary=:all:` 验证
# 过才钉死的——上次那次生产事故（pandas 在 cp313 下没有预编译 wheel，
# 把整个容器卡死）本质原因就是"对着 A 平台验证过的版本号，直接套到了另一个
# Python 版本"。现在多了一个部署目标（这个 bot，大概率是 Vercel），就多了
#一个可能不一样的 Python 版本，同样的陷阱会再踩一次——尤其 gspread/
# google-auth 这些依赖里也有编译扩展（cffi、grpc 相关）。
#
# 顺序：先确认实际部署平台的 Python 版本，再用同一套 pip download 方法
# 验证这里每个包在那个版本下都有预编译 wheel，最后才钉版本号。在那之前，
# 不要在这个文件里加任何 == 版本号。

openai
gspread
google-auth
